<?xml version="1.0"?>
<!DOCTYPE pdf PUBLIC "-//big.faceless.org//report" "report-1.1.dtd">
	<pdf>
		<head>
			<link name="NotoSans" type="font" subtype="truetype" src="${nsfont.NotoSans_Regular}" src-bold="${nsfont.NotoSans_Bold}" src-italic="${nsfont.NotoSans_Italic}" src-bolditalic="${nsfont.NotoSans_BoldItalic}" bytes="2" />
			<#if .locale == "zh_CN">
			<link name="NotoSansCJKsc" type="font" subtype="opentype" src="${nsfont.NotoSansCJKsc_Regular}" src-bold="${nsfont.NotoSansCJKsc_Bold}" bytes="2" />
			<#elseif .locale == "zh_TW">
			<link name="NotoSansCJKtc" type="font" subtype="opentype" src="${nsfont.NotoSansCJKtc_Regular}" src-bold="${nsfont.NotoSansCJKtc_Bold}" bytes="2" />
			<#elseif .locale == "ja_JP">
			<link name="NotoSansCJKjp" type="font" subtype="opentype" src="${nsfont.NotoSansCJKjp_Regular}" src-bold="${nsfont.NotoSansCJKjp_Bold}" bytes="2" />
			<#elseif .locale == "ko_KR">
			<link name="NotoSansCJKkr" type="font" subtype="opentype" src="${nsfont.NotoSansCJKkr_Regular}" src-bold="${nsfont.NotoSansCJKkr_Bold}" bytes="2" />
			<#elseif .locale == "th_TH">
			<link name="NotoSansThai" type="font" subtype="opentype" src="${nsfont.NotoSansThai_Regular}" src-bold="${nsfont.NotoSansThai_Bold}" bytes="2" />
			</#if>
			<macrolist>
				<macro id="nlheader">
					<table   class="header">
						<tr  >
							<td padding-bottom="5px" vertical-align="middle">
								<img src="${companyInformation.logoUrl}"/>
							</td>
						</tr>
					</table>
				</macro>
			    <macro id="nlfooter">
															<#if !paymentlinktext.printinginhtml>
															<#assign br_string="<br />
															">
															<#assign comma_space_string=", ">
															<table width="100%">
																<tr>
																	<td width="75%">&nbsp;</td>
																	<td width="25%">&nbsp;</td>
																</tr>
																<tr>
																	<td style="vertical-align: top;">
																		<p class="small-block-of-text">
																			<span style="font-weight: bold;">${paymentlinktext.contactinfoheader}</span>
																			<br/>
																			${paymentlinktext.emaillabel}${(preferences.subsidiaries)?then(subsidiary.email, companyinformation.email)}
																			<br/>
																			${paymentlinktext.phonelabel}${(preferences.subsidiaries)?then(subsidiary.fax, companyinformation.fax)}
																		</p>
																	</td>
																	<td style="vertical-align: top;">
																		<#if record.paymentlink?length != 0>
																		<p class="small-block-of-text">
																			<span style="font-weight: bold;">${paymentlinktext.paymentlinkheader}</span>
																			<br/>
																			${paymentlinktext.paymentlinkfootertext}
																		</p>
																		</#if>
																	</td>
																</tr>
																<tr>
																	<td colspan="10">&nbsp;</td>
																</tr>
																<tr class="solid-border-top">
																	<td colspan="8" style="font-size: 8px; color: #767676;">
																		<#if preferences.subsidiaries>
																		<#if subsidiary.legalname?length != 0>
																		${subsidiary.legalname},&nbsp;
																		<#else>
																		${subsidiary.name},&nbsp;
																		</#if>
																		<#if subsidiary.mainaddress_text?has_content>
																		${subsidiary.mainaddress_text?replace(br_string, comma_space_string)},&nbsp;
																		</#if>
																		<#else>
																		${companyinformation.companyname},&nbsp;
																		<#if companyinformation.addresstext?has_content>
																		${companyinformation.addresstext?replace(br_string, comma_space_string)},&nbsp;
																		</#if>
																		</#if>
																		<#if record.subsidiarytaxregnum?has_content && record.subsidiarytaxregnum?length != 0>
																		<#list subsidiary.taxregistration as taxreg>
																		<#if taxreg?is_first>
																		${taxreg.taxregistrationnumber@label}
																		<#break>
																		</#if>
																		</#list>
																		: ${record.subsidiarytaxregnum}
																		<#else>
																		<#if preferences.subsidiaries>
																		${subsidiary.federalidnumber@label}: ${subsidiary.federalidnumber}
																		<#else>
																		${companyinformation.employerid@label}: ${companyinformation.employerid}
																		</#if>
																		</#if>
																	</td>
																	<td colspan="2" align="right" style="font-size: 12px;">
																		<pagenumber/>
																		/
																		<totalpages/>
																	</td>
																</tr>
															</table>
															</#if>
				</macro>
			</macrolist>
			<style>
				* {
				<#if .locale == "zh_CN">
				font-family: NotoSans, NotoSansCJKsc, sans-serif;
				<#elseif .locale == "zh_TW">
				font-family: NotoSans, NotoSansCJKtc, sans-serif;
				<#elseif .locale == "ja_JP">
				font-family: NotoSans, NotoSansCJKjp, sans-serif;
				<#elseif .locale == "ko_KR">
				font-family: NotoSans, NotoSansCJKkr, sans-serif;
				<#elseif .locale == "th_TH">
				font-family: NotoSans, NotoSansThai, sans-serif;
				<#else>
				font-family: NotoSans, sans-serif;
				</#if>
				}
				td p {
					align:left;
					line-height: 16px;
				}
				img {
					height: 60px;
					width: 180px;
				}
				a {
					color: blue;
				}
				.header {
					border-collapse: collapse;
					width: 100%
				}
				.document-header-row {
					vertical-align: top;
				}
				.small-block-of-text {
					font-size: 10px;
					line-height: 14px;
				}
				.item-line-height {
					line-height: 11px;
				}
				.breakdown tr {
					font-size: 8px;
					height: 20px;
				}
				.breakdown td {
					vertical-align: middle;
				}
				.light-border {
					border-top: 1px solid #CCCCCC;
					border-bottom: 1px solid #CCCCCC;
				}
				.light-border-bottom {
					border-bottom: 1px solid #CCCCCC;
				}
				.solid-border {
					border-top: 1px solid;
					border-bottom: 1px solid;
				}
				.solid-border-top {
					border-top: 1px solid;
				}
				.background {
					z-index: -100;
					position: absolute;
					top: -6;
					left: 27;
				}
				.foreground {
					z-index: 100;
				}
				.url {
					color: blue;
				}
			</style>
	    </head>
		
		<body header="nlheader" header-height="11%" footer="nlfooter" footer-height="95pt" padding="0.5in 0.5in 0.5in 0.5in" size="A4">
			<table class="header"  >
				<tr>
					<td padding-bottom="5px" align="left">
						<p text-align="left">
							<br/>
							<span style="font-size: 12px; font-weight: bold;">
								<#if preferences.subsidiaries>
								${subsidiary.mainaddress_text}
								<#else>
								${companyinformation.addresstext}
								</#if>
							</span>
						</p>
					</td>
				</tr>
				<tr>
					<td width="55%" style="border: 1px solid red;" bgcolor="#f2f2f2"></td>
					<td width="45%" style="border: 1px solid red;" bgcolor="#f2f2f2">
						<table style="width:100%; border-collapse:collapse; font-size:11px; line-height:12px;">
							<#if record.trandate?has_content>
							<tr>
								<td style="width:55%; text-align:left; padding:1px 3px;"><b>${record.trandate@label}</b></td>
								<td style="width:45%; text-align:left; padding:1px 3px;">${record.trandate}</td>
							</tr>
							</#if>
							<#if record.postingperiod?has_content>
							<tr>
								<td style="width:55%; text-align:left; padding:1px 3px;"><b>${record.postingperiod@label}</b></td>
								<td style="width:45%; text-align:left; padding:1px 3px;">${record.postingperiod}</td>
							</tr>
							</#if>
							<#if record.tranid?has_content>
							<tr>
								<td style="width:55%; text-align:left; padding:1px 3px;"><b>${record.tranid@label}</b></td>
								<td style="width:45%; text-align:left; padding:1px 3px;">${record.tranid}</td>
							</tr>
							</#if>
							<#if record.duedate?has_content>
							<tr>
								<td style="width:55%; text-align:left; padding:1px 3px;"><b>${record.duedate@label}</b></td>
								<td style="width:45%; text-align:left; padding:1px 3px;">${record.duedate}</td>
							</tr>
							</#if>
							<#if record.entity.vatregnumber?has_content>
							<tr>
								<td style="width:55%; text-align:left; padding:1px 3px;"><b>${record.entity.vatregnumber@label}</b></td>
								<td style="width:45%; text-align:left; padding:1px 3px;">${record.entity.vatregnumber}</td>
							</tr>
							</#if>
						</table>
					</td>
				</tr>
			</table>

			<!-- INVOICE SECTION -->
			<table style="width:100%; border:1px solid #ff6600; border-collapse:collapse; margin-top:15px;">
			<!-- Section title -->
			<tr>
				<td colspan="2" style="padding:8px 6px;">
				<span style="font-size:14pt; font-weight:bold;">INVOICE</span>
				</td>
			</tr>
			<tr>
				<td colspan="2" style="border-bottom:1px solid #000000;"></td>
			</tr>

			<!-- Header row -->
			<tr>
				<td style="width:75%; border-bottom:1px solid #000000; padding:5px 6px; font-weight:bold; font-size:10pt;">
				Description
				</td>
				<td style="width:25%; border-bottom:1px solid #000000; padding:5px 6px; font-weight:bold; font-size:10pt; text-align:right;">
				Amount in EUR
				</td>
			</tr>

			<!-- Line items (loop through items or custom lines) -->
			<#list record.item as item>
				<tr>
				<td style="padding:4px 6px; font-size:9pt;">
					${item.description}
				</td>
				<td style="padding:4px 6px; font-size:9pt; text-align:right;">
					${item.amount}
				</td>
				</tr>
			</#list>

			<!-- Optional: Add special notes below items -->
			<tr>
				<td colspan="2" style="padding:4px 6px; font-size:9pt;">
				Billing time zone: CET/CEST (GMT +1:00 / GMT +2:00)
				</td>
			</tr>
			</table>

		</body>
	</pdf>